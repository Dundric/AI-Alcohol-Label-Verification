# Azure OpenAI Configuration (REQUIRED)
# Get these values from Azure Portal > Azure OpenAI Service > Keys and Endpoint
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_DEPLOYMENT=your-deployment-name

# Azure Blob Storage Configuration (OPTIONAL)
# Only needed if you want to use cloud storage features
# Get these from Azure Portal > Storage Account > Access Keys
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your-account;AccountKey=your-key;EndpointSuffix=core.windows.net
AZURE_STORAGE_CONTAINER_NAME=label-images

# Azure Function (OPTIONAL)
# Only needed if you're using an external Azure Function for processing
AZURE_FUNCTION_VERIFY_IMAGE_URL=https://your-function-app.azurewebsites.net/api/verify-image
